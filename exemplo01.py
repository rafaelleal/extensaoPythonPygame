import pygame
pygame.init()
resolucao = (1024, 768) #tupla que define a resolução a ser utilizada na tela do jogo
tela = pygame.display.set_mode(resolucao) #criar a tela do jogo
pygame.display.update() #desenhar a tela